# AI-assistant
AI chat app built with vanilla JavaScript and Node.js. Uses Groq API (Llama 3.1) as the AI backend. The server proxies requests to keep the API key secure. Built as a practice project to learn REST APIs, async/await, and basic backend development.
